import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs'

import MenuItem from './index'

export const Template = (args) => <MenuItem {...args} />

<Meta
  title="Components/MenuItem"
  component={MenuItem}
  parameters={{ controls: { sort: 'requiredFirst' } }}
/>

# MenuItem

## Examples

Here are some examples of using the component.

<Canvas>
  <Story name="0_0" args={{ children: 'Menu Item', href: '#' }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Active MenuItem

<Canvas>
  <Story name="Block" args={{ children: 'Active', href: '#', isActive: true }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Props

<ArgsTable component={MenuItem} />
