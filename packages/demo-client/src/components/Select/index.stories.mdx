import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs'

import withState from 'hoc/with-state.tsx'

import Option from '../Option'

import Select from './index'

export const SelectWithState = withState(Select)
export const Template = (args) => (
  <div style={{ padding: '0 0 200px' }}>
    <SelectWithState {...args} />
  </div>
)

<Meta
  title="Components/Select"
  component={Select}
  parameters={{ controls: { sort: 'requiredFirst' } }}
/>

# Select

## Examples

Here are some examples of using the component.

<Canvas>
  <Story
    name="0_0"
    args={{
      children: [
        <Option value="bandy">Bandy</Option>,
        <Option value="baseball">Baseball</Option>,
        <Option disabled value="basketball">
          Basketball
        </Option>,
        <Option disabled value="bicycling">
          Bicycling
        </Option>,
        <Option value="f1">F1</Option>,
        <Option value="football">Football</Option>,
        <Option value="icehockey">Ice Hockey</Option>,
        <Option value="motorsport">Motorsport</Option>,
        <Option value="tennis">Tennis</Option>,
        <Option value="volleyball">Volleyball</Option>,
      ],
      placeholder: 'Choose your favorite sport',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Props

<ArgsTable components={{ Select, Option }} />

## Types

```typescript
type Props = Omit<JSX.IntrinsicElements['option'], 'children' | 'value'> & {
  children?: string
  value: number | string
}
type Children = { props: Props } | { props: Props }[]
type Value = number | string | null | undefined
```
